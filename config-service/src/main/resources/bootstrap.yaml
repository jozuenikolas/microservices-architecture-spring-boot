server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git: 
          uri: https://github.com/jozuenikolas/microservices-architecture-spring-boot.git
          searchPaths: 
            - config-data
          
  security:
    user:
      name: root
      password: s3cr3t
